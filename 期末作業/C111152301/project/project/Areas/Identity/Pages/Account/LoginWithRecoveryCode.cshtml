@page
@model LoginWithRecoveryCodeModel
@{
    ViewData["Title"] = "恢復碼驗證";
}

<h1>@ViewData["Title"]</h1>
<hr />
<p>
    您已請求使用恢復代碼登入。在您提供之前，系統不會記住此登入信息
    登入時的身份驗證器應用程式代碼或停用雙重身份驗證並再次登入。
</p>
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger" role="alert"></div>
            <div class="form-floating mb-3">
                <input asp-for="Input.RecoveryCode" class="form-control" autocomplete="off" placeholder="RecoveryCode" />
                <label asp-for="Input.RecoveryCode" class="form-label"></label>
                <span asp-validation-for="Input.RecoveryCode" class="text-danger"></span>
            </div>
            <button type="submit" class="w-100 btn btn-lg btn-primary">登入</button>
        </form>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}